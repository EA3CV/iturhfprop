from setuptools import setup, find_packages

setup(
    name='iturhfprop',
    version='1.0.0',
    description='Python wrapper for ITU-R HF propagation prediction',
    author='EA3CV',
    packages=find_packages(),
    install_requires=[],
)

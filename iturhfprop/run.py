def run(txlat, txlon, rxlat, rxlon, txconf, rxconf, freqMHz, month, utchour):
    # Esto es un stub: reemplazar con la lógica real basada en ITURHFProp
    rel = 85  # dummy reliability
    snr = 20  # dummy SNR
    return {"rel": rel, "snr": snr}
